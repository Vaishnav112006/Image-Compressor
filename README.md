# Image Compressor Web (Full)

Educational demo of a JPEG-like compressor with a web UI.

Run inside a venv:

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py

Open http://127.0.0.1:5000
